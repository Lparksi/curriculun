// 课程表应用的 Widget 测试
// 由于应用使用了 Web 特定的依赖（web_file_utils），
// 在非 Web 平台上运行测试会导致编译错误。
// 这里暂时提供一个基础的测试框架，实际测试在 export_service_test.dart 中。

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder test', () {
    // 这是一个占位测试，确保 test 套件不为空
    expect(true, isTrue);
  });
}
